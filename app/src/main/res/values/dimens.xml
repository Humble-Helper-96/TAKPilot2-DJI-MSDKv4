<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
    <dimen name="fab_margin">16dp</dimen>

    <!-- UI Text Size -->
    <dimen name="textsize_connection">20dp</dimen>

    <!-- List View Relative -->
    <dimen name="item_arrow_width">26dp</dimen>
    <dimen name="item_arrow_height">44dp</dimen>
    <dimen name="item_min_height">44dp</dimen>
    <dimen name="item_value_size">18sp</dimen>
    <dimen name="item_title_size">18sp</dimen>
    <dimen name="test_log_textsize">17sp</dimen>
    <dimen name="button_widget">200dp</dimen>
    <!-- Flight-screen mini-map edge. The HUD's right-hand column is the tightest space in the
         app: toolbar, exposure block, telemetry and map all share one landscape screen height,
         and the map is the single largest item. This default is sized for SHORT screens (a
         Samsung S21 is ~411dp tall in landscape); values-h440dp gives taller devices the bigger
         map. Shrinking the map rather than the text keeps the readout legible in daylight,
         which is the part that actually has to work in the field. -->
    <!-- ⚠ REDUCED 2026-08-14 to pay for the HUD panels (conformance D4). The two panels add
         4dp of padding at the top and bottom of each, 16dp in total, and this bucket had only
         5dp of slack in the worst case (FAA ceiling visible). Worst case is now:

             paddingTop 60 + EV slider 24 + map 110 + paddingBottom 12 + margins 20
               + 16 x 10 text lines + 2 panels x 8dp padding = 402dp

         against 411dp of screen, so 9dp of slack.

         ⚠ RE-COUNTED 2026-09-13 FOR THE HUD REFRESH, AND IT CAME OUT LEVEL:

             2 panels x 8dp of vertical padding       =16dp   (the panels are gone; §4.3)
             7 outlined views x about 1.6dp           +11dp   (OutlinedTextView's own padding)
             the height size hierarchy                + 4dp   (§4.4, net of the smaller unit
                                                               and position line)
                                                      =======
                                                       =1dp

         The panels were paying for the outline and did not know it. The vertical 4dp they
         supplied is DELIBERATELY NOT restored on the blocks === see the flight layout === because
         that is the 16dp above, and without it this refresh would not fit on this screen.

         ⚠ THIS IS THE SHORTEST SCREEN OF THE THREE and this is still arithmetic, not a
         measurement. MEASURE IT with `dumpsys activity top` on the phone before release. The
         failure mode is the mini-map clipped SILENTLY at the bottom of the column. One more
         outlined readout costs about 1.6dp. The conformance note on the D1 fix
         names exactly this remedy: take the height from flight_map_size, never by re-joining
         the readout lines. **This is arithmetic, not a measurement.** -->
    <!-- 137dp, MEASURED on the S20 Ultra 2026-09-14 (operator): at 110dp the map's left edge
         sat 80px = 30dp to the right of where the 16:9 picture ends (1920px in a 2326px
         window — FpvTextureView pillarboxes fit-left, so that strip is black). The column is
         the map's width, so this also widens the EV slider and the scale row to the same
         edge. 137 puts the map's left edge about 4dp off the picture. The HEIGHT is held at
         110dp below: the column had 11px of slack, and width is free here where height is not. -->
    <dimen name="flight_map_size">137dp</dimen>
    <!-- Keeps the resource row clear of the HUD column: map width plus the column's end
         padding (137 + 14). Tracks flight_map_size by hand, so change both together. -->
    <dimen name="flight_resource_row_margin_end">151dp</dimen>
    <!-- Separate from flight_map_size so a wider column need not be a taller one. NOT equal
         to the width since v1.2.10: the map is 137x140dp. MEASURED (2026-09-14): with the
         RTH/HOME/FAA lines tightened the column's weighted spacer held 57px at 110dp of map;
         18dp more map left about 10px, and the last 12dp came from the column's paddingBottom
         going to zero so the map's bottom edge lines up with the picture's (operator), and 14dp
         more from the toolbar band shrinking 60 to 45dp in v1.2.11. Every dp
         added here comes straight out of that spacer; measure before touching it (D20). -->
    <dimen name="flight_map_height">154dp</dimen>
    <!-- HUD readout size, per bucket. 12sp is what hudText already used, so this
         changed no pixel; it exists so a controller can raise it (conformance D7).
         On a sunlight-readable screen at arm's length 12sp is too small. -->
    <dimen name="flight_readout_text_size">12sp</dimen>

    <!-- HUD CHROME DIMENSIONS === specification §7, ported from the Autel sibling 2026-09-13.

         ⚠ THIS TREE'S BASE BUCKET IS A PHONE. The Autel controller draws readouts at 18sp and
         this target draws them at 12sp, so these are NOT that tree's numbers. They match the
         MSDKv5 sibling, which also draws at 12sp. -->

    <!-- The one edge weight the whole HUD shares === readouts, EV slider, map frame. About an
         EIGHTH of the readout text size.
         ⚠ TOO WIDE AND THE READOUTS TURN TO BLACK BLOBS: the stroke is CENTRED on the glyph
         edge, so half of it falls inside the letter and is covered by the fill. -->
    <dimen name="hud_text_outline_width">1.5dp</dimen>

    <!-- The shape of every toggle pill, and of the mini-map's corners.
         ⚠ 8dp IS NOT hud_control_radius. The capsule is the container and takes the large
         radius; the pills inside it take the small one, and so does the map, which is read
         rather than pressed. -->
    <dimen name="hud_pill_radius">8dp</dimen>
    <dimen name="hud_pill_stroke">1.5dp</dimen>

    <!-- The floating toolbar capsules. Large radius: "you touch this". -->
    <dimen name="hud_control_radius">20dp</dimen>

    <!-- ⚠ BOTH TOOLBAR CAPSULES MUST BE THIS TALL, AND IT IS A MINIMUM, NOT A HEIGHT.
         The two clusters hold different things, so they measure differently: the ACTIONS
         capsule's tallest child is the 46dp drop pin and the STATUS capsule's is a 34dp icon.
         Two capsules that begin on the same line and end on different ones read as a mistake.

         ⚠ IT IS DERIVED, NOT CHOSEN: the actions capsule's natural height, which is the drop
         pin plus the capsule's own paddingVertical on each side.

             46dp drop pin + 2 x 3dp paddingVertical = 52dp

         A value BELOW that does nothing at all === the actions capsule is already taller, and
         the status capsule stays short. It was 40dp in the DJI trees from the 2026-09-13 port
         until this was caught by comparing the three trees, and the capsules were not level.
         If the drop pin or the padding changes, this changes with them.

         A MINIMUM rather than a fixed height because either cluster may still grow: a longer
         signal readout, a taller button style, a density not yet seen. minHeight can only make
         a capsule taller, thus it can never clip its own content. -->
    <!-- 40dp since v1.2.11: the toolbar is a quarter shorter (operator, 2026-09-14) — the drop
         pin is 36dp, every other pill and icon 26dp, the capsules pad 2dp, the band 5dp. Still
         DERIVED: the drop pin plus its padding. The band is 45dp and so is the column's
         paddingTop. ⚠ §6.7 says 34dp pills; this phone runs 26 — a per-device departure the
         operator took, recorded on ledger D20. -->
    <dimen name="hud_capsule_height">40dp</dimen>
    <!-- ONE text size for every pill that carries a word — AR, 1X, LIVE, REC (operator,
         2026-09-14). The two custom views used to size their word from the pill's height
         (0.4 of it), which at 26dp came out near 10sp against the TextViews' 14sp. -->
    <dimen name="hud_pill_text_size">14sp</dimen>
    <!-- ONE gap between neighbouring pills in the actions capsule (operator, 2026-09-14). The
         gaps were 10, 18, 8, 6 and 12dp, each pill carrying its own margins from a different
         day. Every pill now has this on its start edge only; the capsule's own padding closes
         the ends. -->
    <dimen name="hud_pill_gap">8dp</dimen>

    <!-- The warning banner, PER-DEVICE (specification §4.8): text size and maximum width. These
         were literals in the layout until v1.2.13. Narrowing the banner makes it TALLER, not
         smaller — reduce the text first. -->
    <dimen name="flight_warning_text_size">15sp</dimen>
    <dimen name="flight_warning_max_width">380dp</dimen>
    <!-- The ✕ that closes the banner: the touch-target minimum, not the glyph. A 15sp ✕ is about
         10dp across and not hittable with a thumb over live video. -->
    <dimen name="flight_warning_close_size">34dp</dimen>

</resources>
